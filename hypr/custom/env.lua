-- Cursor theme for XWayland / toolkits that read the X env vars.
-- The Wayland-side theme is set with `hyprctl setcursor` in custom/execs.lua.
hl.env("XCURSOR_THEME", "Future-cyan-cursors")
hl.env("XCURSOR_SIZE", "30")
