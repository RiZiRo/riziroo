import QtQuick

/**
 * OpenAI-compatible strategy for Anthropic-backed endpoints. Behaviour is
 * identical to OpenAiCompatibleStrategy; kept as its own type so the
 * "claude" api_format can be pointed somewhere if the proxies diverge.
 */
OpenAiCompatibleStrategy {}
