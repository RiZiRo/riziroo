import QtQuick

/**
 * OpenAI's own chat-completions API. All behaviour lives in
 * OpenAiCompatibleStrategy; this exists so model definitions can name a
 * strategy per api_format.
 */
OpenAiCompatibleStrategy {}
